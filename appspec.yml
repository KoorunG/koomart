version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/kotlin-toy-project
permissions:
  - object: /home/ubuntu/kotlin-toy-project/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu